# Core dependencies
fastapi>=0.104.0,<1.0.0
pydantic>=2.0.0,<3.0.0
uvicorn[standard]>=0.24.0,<1.0.0

# Document processing
pdfplumber>=0.10.0  # PDF text extraction (license-safe alternative to pymupdf)
python-docx>=1.1.0  # Word document (.docx) text extraction
sentence-transformers>=2.2.0  # Embedding models

# LLM clients (optional - install as needed)
# Azure OpenAI
openai>=1.0.0

# Anthropic
anthropic>=0.7.0

# Token counting (for TOON format token measurement)
tiktoken>=0.5.0

# Utilities
python-multipart>=0.0.6  # For FastAPI file uploads
beautifulsoup4>=4.12.0  # HTML parsing for web crawler
requests>=2.31.0  # HTTP requests for web crawler
urllib3>=2.0.0  # URL parsing and robots.txt support
ddgs>=0.1.0  # Web search (DuckDuckGo - free, no API key required)

# Vector database support
chromadb>=0.4.0  # For ChromaDB support
psycopg[binary]>=3.1.0  # PostgreSQL driver for pgvector
pgvector>=0.4.0  # pgvector extension support
azure-cosmos>=4.5.0  # For Cosmos DB support
leann>=0.3.0  # LEANN low-storage vector index (optional, for RAG_MODE=leann)

