# Migration Mapping

## Page → Component Mapping
- Map each `.aspx` to an Angular component and route.

## Code-behind → API Mapping
- Map server-side methods to controller endpoints and services.

## Control Mapping
- Map server controls to Angular Material / custom components.

## State & Session Mapping
- ViewState → component/local state
- Session → JWT + server-side session store (if needed)
