# Sample Components

This folder is a placeholder for example component implementations generated by the `angular-component-generator` skill.

Add sample component folders here for reference (e.g., `product-list/`, `user-profile/`).
