# Sample API

This folder is a placeholder for example generated API feature implementations (e.g., `Product/`, `Order/`).

Place generated Controllers, Services, Repositories, DTOs, Entities, Validators, and Tests here for reference.
